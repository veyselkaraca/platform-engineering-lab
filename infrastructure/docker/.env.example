# Sample values only — clearly fake. Copy to .env (git-ignored) and adjust.
POSTGRES_USER=lab
POSTGRES_PASSWORD=change-me-lab-only
RABBITMQ_DEFAULT_USER=lab
RABBITMQ_DEFAULT_PASS=change-me-lab-only
USER_SERVICE_HOST_PORT=3001
ORDER_SERVICE_HOST_PORT=3002
RABBITMQ_MANAGEMENT_HOST_PORT=15672
NOTIFICATION_WORKER_HOST_PORT=3003
